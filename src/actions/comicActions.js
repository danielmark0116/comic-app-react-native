import * as types from "./actionTypes";

export const comicGetList = payload => ({
  type: types.TEST,
  payload
});

// THUNKS
export const comicGetListThunk = () => {
  return async dispatch => {
    //
  };
};
